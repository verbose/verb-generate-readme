Install [update][] if it isn't already installed:

```sh
$ npm install --global update
```

Install the `{%= name %}` generator:

```sh
$ npm install --global {%= name %}
```

Run run the default task in `{%= name %}`:

```sh
$ gen {%= alias %}
```
