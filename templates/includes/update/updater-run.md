Make sure your work is committed, then run the updater's `default` [task]({%= contentLink('update/update', 'docs/tasks.md#default-task') %}) with the following command:

```sh
$ update {%= replace(name, /updater-/, '') %}
```
