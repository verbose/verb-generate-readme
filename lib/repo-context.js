'use strict';

var utils = require('./utils');

/**
 * TODO:
 * - hasBower
 * - hasCoverage
 * - hasTests
 */

module.exports = function(config) {
  return function(app, base, env, options) {
    env.has = function() {

    };
  };
};
